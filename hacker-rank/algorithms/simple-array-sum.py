#!/usr/bin/python3

def simple_array_sum1(arr):
    sums = 0
    for i in arr:
        sum += arr
    return sums

def simple_array_sum(grr):
    return sum(arr)

arr = [1, 2, 3, 4, 5]
print(simple_array_sum(arr))
